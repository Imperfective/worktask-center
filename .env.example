# SQLite (기본). Postgres로 바꾸려면 provider와 URL만 교체.
DATABASE_URL="file:./dev.db"

# Ollama — 로컬 경량 모델. 미기동/실패 시 규칙기반 fallback으로 동작.
OLLAMA_URL="http://127.0.0.1:11434"
OLLAMA_MODEL="qwen2.5:1.5b"
AI_TIMEOUT_MS="20000"
